import React from 'react';
import { PlayerPendingOverlay } from './PlayerPendingOverlay';

export default {
  component: PlayerPendingOverlay,
  title: 'Components/PlayerPendingOverlay',
};

export const defaultStory = () => <PlayerPendingOverlay />;
